# todo/

Plans for work that is **not finished**. Documentation of the system as it *is* belongs in
[`../research/`](../research/).

The test is one question: **is someone still supposed to build this?** If yes it lives here; once it
ships, the plan moves to `research/` with its status changed to `IMPLEMENTED <date>` and its
deviations recorded.

Every plan starts with a status line on line 2–3 and carries: the symptom or goal before any
solution, references to real code as `file.ts:line` / `file.cs:line` (verified, not guessed), a build
order, a test plan, and a Definition of Done.

## Currently open

| plan | status | scope |
|---|---|---|
| [PLAN_tails_2.md](PLAN_tails_2.md) | plan only | Successor to [PLAN_tails.md](../research/PLAN_tails.md), and a different kind of list: it holds the questions that **cannot be answered by code**, plus one place where the product promises more than it does. The three decisions: what the `edit` rung should mean now that an agent can create and delete an entry but not rename one (the widest gap on the whole surface); whether SSH key generation opens for CREATION, since the recorded refusal argues about rotation and does not carry; and whether a one-time code is returned or APPLIED — returning it would be the first tool handing back something derived from a secret. Then the Linux item: `keyringWarning.ts` guesses from one environment variable and sends the reader to a README. It should ASK D-Bus (absent / present-but-locked / working are three different answers), offer the exact install command for the distribution, run it visibly, and **re-diagnose** afterwards — a package manager's exit code is not a working Secret Service. And it must name WHICH machine: `extensionKind: ["ui"]` pins the host to the local computer, so under Remote-SSH the box to fix is not the one you connected to |
| [PLAN_agent_folder_ops_itest.md](PLAN_agent_folder_ops_itest.md) | plan only | The one open item of [../research/PLAN_agent_folder_ops.md](../research/PLAN_agent_folder_ops.md): the folder verbs are proven in halves — unit tests of the decisions on both sides, and a live `tools/list` showing all four — but nothing yet drives `creds_create_folder` through a real broker to real storage. Six steps for `creds-mcp-itest.cjs`, one of which has teeth: a request carrying `mcp` in its body must leave the switches untouched, and replacing the field-by-field read with a spread must turn it red |
| [PLAN_session_timesheet.md](PLAN_session_timesheet.md) | plan only | A timesheet draft that writes itself from what Claude Code already records: `~/.claude/projects/<cwd-slug>/<sessionId>.jsonl` carries every prompt and reply with `timestamp`, `cwd` and `gitBranch` (verified 2026-08-27 — 957 transcripts for one repo; one file ~145k tokens, so the reader streams). Deterministic code does everything that produces a number — incremental scan, gap-merged activity intervals, project attribution, git evidence, a masked and capped digest — and an LLM appears at exactly one step, wording the one-line description, with a deterministic fallback so it can be absent entirely. Hours can never come from the model. Off by default, per-repo opt-in, nothing leaves the machine; the confirmed `{hours, line}` is for the future rsdPayroll (spec: ClaudeRag `TZ_payroll_en.md` §10.1), whose submission is a different plan in a different repo. Placement is owner decision 1: recommended as a separate private extension `src_timesheet/`, the seed of the payroll developer surface |
| [ЗАДАЧА_проверка_mcp_вручную.md](ЗАДАЧА_проверка_mcp_вручную.md) | не сделано | Ручная проверка всего MCP (0.65.0–0.76.0) за один заход, ≈30 минут, по-русски: настоящий Claude Code против настоящего окна согласия. Автотесты проверяют половины; здесь то, что может только человек. Ключевой пункт — №4: `creds_rotate` со `statement: echo '{{creds:new}}'` должен вернуть агенту `<CREDS_MASKED:PASSWORD>`, а не пароль (регрессия, найденная и починенная в 0.76.0). Пункт 11 ждёт тега `mcp-v*` — релиза ещё нет, кнопку установки проверить не на чем |
| [ЗАДАЧА_проверка_корп_восстановления.md](ЗАДАЧА_проверка_корп_восстановления.md) | не сделано | Ручная проверка корпоративного восстановления втроём — то, что автотесты задать не могут. Главный вопрос пункта 7: доля офицера лежит в SecretStorage, который **не синхронизируется**, поэтому «принял на ноутбуке — участвую с десктопа» может честно не работать; ответ в любую сторону надо записать в интерфейс, а не выяснять в аварии. Плюс: переживает ли одноразовый PIN диктовку голосом, и что видит цель, чей сейф перекеили под PIN, которого она не знает |
| [PLAN_org_recovery_tail.md](PLAN_org_recovery_tail.md) | plan only | What corporate recovery shipped without: the **live three-machine rehearsal** its own DoD asked for and nobody has run — which is where the questions tests cannot ask live (does an officer who accepted on machine A contribute from machine B, given the share is in non-syncing SecretStorage?) — plus cheap roster rotation that re-splits the existing key instead of re-running the whole ceremony. The trade is recorded rather than assumed: a re-split is right for ADDING an officer and wrong for removing one, because their old share stays valid |
| [ЗАДАЧА_скриншоты_для_маркетплейса.md](ЗАДАЧА_скриншоты_для_маркетплейса.md) | не сделано | Единственный оставшийся пункт [PLAN_marketplace_listing.md](../research/PLAN_marketplace_listing.md), выделенный отдельно и по-русски: четыре (предлагается шесть) картинки в `media/docs/`, все с **выдуманными** данными, ссылки абсолютными raw-GitHub URL, `media/docs/**` исключён из `.vsix`. Снять кадры может только человек |

## Promoted

Implemented plans live in [`../research/`](../research/), newest first.

| plan | landed | what it delivered |
|---|---|---|
| [PLAN_audit_roadmap_2026_08_25.md](../research/PLAN_audit_roadmap_2026_08_25.md) | 2026-08-28 | The 2026-08-25 audit's roadmap, every block: the architectural debt (ESLint in CI, tests for the `vscode` modules, the kind discriminant and now the shapes union, the diagnostics channel, AAD on the envelope, `extension.ts` 5689 → 1105), the defects, the performance work, eleven features each by its own plan, and the server tail. D9 (2SKD) dropped by the owner. |
| [PLAN_server_ops.md](../research/PLAN_server_ops.md) | 2026-08-28 | The server's operational hardening, all nine items: restart/backup/restore runbooks, inbox pruning, optimistic concurrency, the contract version, share withdrawal — and on the last day a network-FS refusal for `DataDir`, the officers' metrics page, the runtime-support log line, the cached health verdict and the byte budget (E1). |
| [PLAN_share_metadata_aad.md](../research/PLAN_share_metadata_aad.md) | 2026-08-28 | A share's label bound to its ciphertext as GCM AAD; legacy shares open, marked unbound, until 0.85.0. The review's last open finding. |
| [PLAN_ephemeral_secrets_tail.md](../research/PLAN_ephemeral_secrets_tail.md) | 2026-08-28 | What 0.59.0 left of its own DoD: *Burn Now…* on short-lived entries, the lifetime on the card, and the cross-machine burn as a test — a burn on A is gone on B after a sync, and an old backup does not resurrect it. |
| [PLAN_mcp_wsl_bridge.md](../research/PLAN_mcp_wsl_bridge.md) | 2026-08-28 | `creds-mcp` inside WSL reaches a window on Windows with no configuration: the Linux binary hands the whole session to `creds-mcp.exe` through interop and carries its stdio (`WslPump`), with its own `WslInterop.CredsMcp` bridge and `CREDS_MCP_WINDOWS_BINARY`. Two deviations recorded: the plan’s buffering prediction was **measured and refuted**, and its echo-script integration test is impossible as written (`ShouldRelay` short-circuits on Windows), so the pump is unit-tested on streams and `scripts/creds-mcp-wsl-itest.cjs` drives the real binary inside a real distribution. It also found `creds-mcp-itest.cjs` broken on master — 19 checks, a positional-argument shift from 32d8f01, unseen because that script is not in CI |
| [PLAN_tails.md](../research/PLAN_tails.md) | 2026-08-28 | The 2026-08-27 audit of every open plan plus everything the owner raised while watching — 34 tails, T1–T34, shipped as 0.80.0 → 0.81.0: viewer/form layout and frames, arrival tint, filters, generators, one highlighter, help in five languages, agent doors, the ratchet, the size of `extension.ts`, Restore from the Trash, the click model. T2's measurement became security-tail item 1. |
| [PLAN_extension_security_tail.md](../research/PLAN_extension_security_tail.md) | 2026-08-28 | Findings 6, 8, 10 and 14 of the 2026-08-23 review: PIN policy, idle auto-lock, a real owner-only ACL on Windows — and the WebAuthn RP ID moved from the bare `localhost` to `creds-for-devs.localhost` with a one-touch re-registration of legacy keys (0.81.0). Finding 7 (share metadata AAD) extracted to its own plan. |
| [PLAN_marketplace_listing.md](../research/PLAN_marketplace_listing.md) | 2026-08-24 | The listing text and metadata: the README opens with a table of what the product does, the manifest carries categories, keywords and the gallery banner. The screenshots are a human task with its own file. |
| [PLAN_logging_convention.md](../research/PLAN_logging_convention.md) | 2026-08-27 | The server's logging brought level with the family rule: the hand-written `AnsiConsoleSink` ported (Serilog's own console theme measurably writes zero escapes once stdout is redirected — and a container's captured stdout always is), `DailyRunFileSink` segmenting a never-restarting run at UTC midnight, and `LogRetention` as the named owner of `logs/` — 14 days, the number the extension had already chosen. The record worth reading: its item 2 was **void, not done** — it asked for a row in a mirror list the shared rule no longer has, because the rule moved into the submodule this repo already mounts |
| [PLAN_qr_seed_paste.md](../research/PLAN_qr_seed_paste.md) | 2026-08-27 | A one-time-code seed arrives as a **pasted picture**, because that is the only form Google Authenticator will ever let one out in. A QR reader written from ISO/IEC 18004 in five modules, so the extension keeps its zero runtime dependencies — the owner chose that over `jsqr` explicitly. What makes it safe is the fixture: forty-one real payloads encoded by a **third-party** encoder, so the standard's tables are checked against somebody else's reading of the standard. The bug worth reading is Berlekamp-Massey written with an `else`: it passes every clean symbol and repairs nothing |
| [PLAN_config_sharing.md](../research/PLAN_config_sharing.md) | 2026-08-27 | Sharing a config carries the document. Extracted and shipped the same day, because the owner tried it and found the JSON did not survive. The record worth reading is the hole it found on the way: `setPassword(undefined)` means "keep what is stored", so an entity converted from a credential into a config kept an invisible, uneditable password — and `isShareable` returned true for anything with one. Such a config was ALREADY shareable and would have delivered the password with the document left behind. `keepsPassword(kind)` now scrubs it on write, as TOTP already did, and `isShareable` names the kind rather than inferring it. Also found and NOT fixed: `buildSharePayload` never reads the TOTP seed, so sharing an existing entity loses its second factor while `hasTotp` claims otherwise |
| [PLAN_config_entities.md](../research/PLAN_config_entities.md) | 2026-08-27 | `appsettings.Development.json` lives in the vault instead of being passed between developers by hand. A `config` entity kind whose BODY is a secret in SecretStorage — a script body is what a person typed, a config body holds connection strings — validation that saves anyway and marks the row `!!!` until it parses, a Fields tab that is a VIEW over the raw text so it cannot lose your formatting, materialising to disk with a git check, a long-lived per-entity key of which only the hash is kept, `POST /v1/config/read` and `creds config <key>`, a key-level diff, and a warning when the repository still has a tracked copy. **The record worth reading is deviation 1**: the .NET package this plan called for was never built. The owner asked whether an instruction would do, and checking showed .NET already ships `AddJsonStream` — the package would have wrapped ten lines and bought a fifth release line. A twenty-language snippet panel in the viewer replaced it. Tail: [PLAN_config_sharing.md](../research/PLAN_config_sharing.md), shipped the same day |
| [PLAN_org_recovery.md](../research/PLAN_org_recovery.md) | 2026-08-27 | Corporate break-glass recovery: an operator names officers, every vault on that server also seals its key to an X25519 escrow wrap, and any quorum can open a departed colleague's vault — the org key split with a hand-written GF(2^8) Shamir and destroyed at setup. The records worth reading are the three things the design got wrong on paper: a spike refuted the JWK route for private keys before a line was written (DER has no such asymmetry), the recovery must try SUBSETS because interpolation over a wrong one returns a well-formed wrong key rather than failing, and the server's threshold gate is a courtesy it structurally cannot enforce — the client's integrity tag is the real one. Tail extracted to [PLAN_org_recovery_tail.md](PLAN_org_recovery_tail.md) |
| [PLAN_recovery_code_wrap.md](../research/PLAN_recovery_code_wrap.md) | 2026-08-27 | A printed recovery code as a third `KeyWrap` — 150 bits of Crockford Base32 with a checksum that names a typo locally, shown once on a page with Print and deliberately no Copy. Two defects found on the way, both red-first: `backupWriteMode` routed by "has a webauthn wrap" and would have silently stripped the new slot, and removing your last security key retired the printed code **in silence** — unavoidable arithmetic, but the silence was the bug. Rotation moved into one `vaultRekey.rekeyUnderPin` |
| [PLAN_cli_bridge_tail.md](../research/PLAN_cli_bridge_tail.md) | 2026-08-27 | The tail the CLI and bridge plans left, closed by the one thing no test could reach: a human clicking *Open Remote Bridge*. It was written as a formality — the transport had already been verified against a real sshd — and the click found that the bridge had never worked at all. Four independent causes, each sufficient on its own: the credential resolver knew one kind of four, so a password entry (the case the bridge exists for) got an `ssh` with nothing to authenticate with; no `BatchMode`, so the resulting refusal became a permanent wait at a password prompt on a pipe; a socket check that saw the absence and filed it at info level, having collapsed "no socket" into "this host has no `stat`"; and a notification announcing success before any of it was checked. A fifth — the menu item still saying *Open* while a bridge ran — was found by the owner in the same click. `creds ls` shipped under a recorded decision, and phase 4 was re-judged rather than built: 4a was a defect (`-A` inert on Windows) and shipped in 0.65.0, 4b became its own plan and was built |
| [PLAN_mcp_server.md](../research/PLAN_mcp_server.md) | 2026-08-27 | MCP: an agent sees, uses, rotates, creates and deletes exactly what you opened to it. Six switches per entry on a ladder, all off by default, inherited from a folder; a separate `creds-mcp` AOT binary so the extension keeps its zero runtime dependencies; rotation through a `{{creds:new}}` placeholder so no value ever enters the agent's context; deletion to the Trash only; and a journal counting the two costs — secrets that came FROM an agent, and secrets we were asked to make and could not. Its WSL tail shipped 2026-08-28: [PLAN_mcp_wsl_bridge.md](../research/PLAN_mcp_wsl_bridge.md) |
| [PLAN_wsl_agent_relay.md](../research/PLAN_wsl_agent_relay.md) | 2026-08-26 | The SSH agent reached from inside WSL, where a Linux kernel cannot open a Windows named pipe: `creds relay` serves a unix socket and gives each connection its own Windows child. The whole design was measured with throwaway scripts BEFORE any C# was written — including that WSL interop pipes do not corrupt binary — and the record worth reading is what the build then taught: one missing backslash in a pipe prefix compiles, matches nothing, and reports itself as a window that has gone; and a `dotnet build` from the repository root reports success while building a different project |
| [PLAN_headless_cli.md](../research/PLAN_headless_cli.md) | 2026-08-26 | `creds` — a .NET Native AOT binary that uses a vault credential from any terminal without receiving it, with named grants so it is `creds ssh prod-db` rather than a pasted token. The record worth reading is what the end-to-end test found AFTER the feature was called done: `/v1/use/exportEnv` was unreachable because the route grammar rejected a capital letter, so the `env` verb had never worked from any client, in any released build. Contract tests on both sides had asserted the two implementations agreed and never that either could reach the broker. `creds ls` is deliberately absent — see [PLAN_cli_bridge_tail.md](../research/PLAN_cli_bridge_tail.md) |
| [PLAN_remote_broker_bridge.md](../research/PLAN_remote_broker_bridge.md) | 2026-08-26 | The broker reachable from **WSL** and from **Remote-SSH**, which the owner required to both work. WSL needs no configuration at all: the Linux binary hands the call to the Windows one through interop, so nothing new listens anywhere. Remote-SSH holds an `ssh -R` open and forwards to the loopback **port** rather than a local socket — the obvious `-R sock:sock` cannot work from Windows, where our endpoint is a named pipe. The honest limit is recorded: on POSIX the socket is a real 0600 boundary, on Windows the pipe carries only the default DACL, which is why the alias route is rate-limited |
| [PLAN_depends_on.md](../research/PLAN_depends_on.md) | 2026-08-26 | An entity says what it depends on, both ends wear one colour in the tree, and the entity depended ON grows a *Depended on by* sub-tree beside its version history — with a button from a folder there to where it really lives. The deviation worth reading: colour borrowing is **one hop, never transitive**, pinned after a test of mine failed asserting the opposite — in a vault where everything eventually reaches one VPN, following the chain would paint every entry alike and stop saying anything. Also recorded: the 800-line lint ceiling forced three extractions before a line of the feature could be written, and an `as` cast in a fixture hid two new required fields from the compiler until eleven tests threw at runtime |
| [PLAN_ephemeral_secrets.md](../research/PLAN_ephemeral_secrets.md) | 2026-08-26 | Entries that delete themselves — 1 hour, 1 day, until VS Code closes, or until an agent has used them once — through the one deletion path, so the secret, its revision history and a causal tombstone all go. The deviation worth reading: "until the window closes" became a **lease**, because a close handler never runs for a window that crashed, and the entry it promised to destroy would then outlive its own promise holding a working secret. Tail extracted to [PLAN_ephemeral_secrets_tail.md](../research/PLAN_ephemeral_secrets_tail.md) |
| [PLAN_ai_context_masking.md](../research/PLAN_ai_context_masking.md) | 2026-08-26 | Vault secrets redacted out of Exec Broker output before it reaches an AI's context, at one interception point that covers every action present and future. The clipboard half is recorded as **refuted, not deferred** — VS Code exposes no clipboard-change event and Windows Clipboard History captures at copy time — and replaced by two on-demand scans. Two later defects are recorded in the plan: MSSQL passwords never entered the mask table (and an existing test had pinned that as intended), and an entry name could smuggle instructions into the agent snippet |
| [PLAN_git_sync_transport.md](../research/PLAN_git_sync_transport.md) | 2026-08-25 | A third transport: the encrypted vault in a private git repo, reusing the causal merge untouched. Deviations recorded in the plan — history retention and the PAT path were left out, and the integration test found that git rewrites line endings on checkout, which would have corrupted every vault it stored on Windows |
| [PLAN_broker_itest_defects.md](../research/PLAN_broker_itest_defects.md) | 2026-08-25 | Two defects that reached released builds and stayed: a refused agent token answered "unknown" instead of "denied" (the registry swept every refusal on the next mint), and three assertions about decrypted key material tested nothing because the itest helper never looked inside `keys/<pid>/`. The finding worth more than either: `agent-broker-itest.cjs` ran in no CI at all — it does now (roadmap A3's first half) |
| [PLAN_import.md](../research/PLAN_import.md) | 2026-08-25 | Import from `~/.ssh/config` and from Bitwarden / 1Password / KeePass / LastPass / Termius exports; KDBX deliberately not built, with the reason recorded |
| [PLAN_generator.md](../research/PLAN_generator.md) | 2026-08-25 | Password, passphrase and Ed25519 generation in the form — a fresh key that never touches disk |
| [PLAN_hygiene_reports.md](../research/PLAN_hygiene_reports.md) | 2026-08-25 | The health report — reused, weak, unencrypted keys, plaintext `.env`, and an opt-in k-anonymous breach check |
| [PLAN_polish_layer.md](../research/PLAN_polish_layer.md) | 2026-08-25 | Keybindings, go-to-credential, `viewsWelcome`, a walkthrough and the lock status-bar item |
| [PLAN_connection_manager.md](../research/PLAN_connection_manager.md) | 2026-08-25 | The connection-manager fields an SSH entity was missing: a jump host as a typed reference (never a `ProxyCommand` string), `-L`/`-R` forwarding rules as rows that can be switched off, agent forwarding and tags — plus audit B10 closed, so a first connection shows the host fingerprint and a pinned key is enforced rather than warned about |
| [PLAN_ssh_agent.md](../research/PLAN_ssh_agent.md) | 2026-08-25 | An SSH agent of our own that serves vault keys from memory and asks before **every** signature, naming what is being signed; `SSH_AUTH_SOCK` in new terminals, so `ssh` and `git` need no configuration and no `0600` file exists at all; *Copy Git Signing Config* for `gpg.format ssh`. Verified against the real `ssh-add` and `ssh-keygen -Y sign` on Windows and WSL |
| [PLAN_secret_references.md](../research/PLAN_secret_references.md) | 2026-08-25 | `creds://account/entity/field` references in commands and scripts, resolved into the **child's** environment only (argv carries a name, never a value) and run in a terminal whose output has every resolved value replaced with `[masked]` — the half `detectSecretPrints` could only warn about. The broker's script action masks what it returns to an agent |
| [PLAN_totp.md](../research/PLAN_totp.md) | 2026-08-25 | TOTP seeds as a first-class secret field — RFC 6238 with the Steam variant, the live code with a countdown in the viewer, *Copy One-Time Code* in the tree; the seed never reaches the webview, only the code derived from it |
| [PLAN_v1_vault_migration.md](../research/PLAN_v1_vault_migration.md) | 2026-08-25 | Retired the v1 vault envelope: every vault is now v3 (wrapped/HKDF, scrypt once at unlock), PIN-only included. A legacy v1 vault migrates on its next sync (same PIN, data preserved); a new PIN-only vault is v3 from the first write. Closes the deferred tail of the 2026-08-25 review's finding 4 |
| [PLAN_nas_sender_pki.md](../research/PLAN_nas_sender_pki.md) | 2026-08-25 | Ed25519 signatures on folder shares with trust-on-first-use pinning and a fingerprint to read aloud — forgery-resistant, deliberately not described as forgery-proof. Teams should still use the server, where the sender is stamped from a verified sign-in |
| [PLAN_agent_ssh_broker.md](../research/PLAN_agent_ssh_broker.md) | 2026-08-24 | *Share with Claude Code…* — an AI agent can run commands on an SSH host and open its terminal without ever receiving the password or key: the window that holds the credential runs `ssh` on the agent's behalf, gated by a first-use consent modal and written down in an audit channel |
| [PLAN_monorepo_consolidation.md](../research/PLAN_monorepo_consolidation.md) | 2026-08-23 | Both products into one conventions-compliant repository, with CI, MIT licensing, Marketplace preparation, a one-command Docker deployment, and a security/reliability review whose five high-severity fixes each carry a test watched failing first |
| [PLAN_sharing.md](../research/PLAN_sharing.md) | 2026-08-21 | Sharing one sealed entity with a colleague |
| [PLAN_audit_followups.md](../research/PLAN_audit_followups.md) | 2026-08-21 | KDF versioning, causal merge, envelope MAC, PIN re-key, remote vault deletion, notes into SecretStorage |

## A note on what belongs here

`todo/` holds plans meant to be executed. It is not a scratchpad: session notes, transient checklists
and "look at this later" reminders do not go here, because a folder that collects those stops being
readable as a list of commitments.
