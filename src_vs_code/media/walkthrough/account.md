## Where everything hangs

An **account** is a profile: its own tree, its own encrypted vault file, its own team.

- Microsoft is built in; Google uses the extension's own OAuth flow.
- Neither is needed to store credentials locally — sign-in is what gives a vault a *place to sync to*, not permission to exist.
- Several accounts live side by side, each with its own sync location.
