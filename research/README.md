# research/

Documentation of the system **as it is** — including design records of plans that already shipped.
Work still to be done lives in [`../todo/`](../todo/).

The test for which folder a document belongs in is one question: *does this describe code that
exists today?*

> Cross-repository citations are written as **paths, not links** (`dew_flow_mcp · research/x.md:12`).
> A relative link that resolves only on one machine is worse than a citation that names its source.

## Start here

| Document | What it answers |
|---|---|
| [architecture.md](architecture.md) | How the extension and the server fit together, and the trust boundary everything follows from |
| [module_extension.md](module_extension.md) | The extension's layers, cryptography, sync algorithm and packaging |
| [module_server.md](module_server.md) | Every endpoint, its authorization rule, the storage layout — **the single statement of the HTTP contract** |
| [module_deployment.md](module_deployment.md) | The container stack, the four TLS modes, updates and backups, and why each is shaped that way |

## Reviews

| Document | Landed |
|---|---|
| [SECURITY_REVIEW_2026-08-25.md](SECURITY_REVIEW_2026-08-25.md) | 2026-08-25 — post-merge review of the agent-broker/v3/signatures/scripts/scope merge across security, performance and resilience. Ten findings confirmed in code and fixed red-first: a psql option-injection on the agent DB path, an auto-lock/sync key-wipe race and a non-atomic backup that could each brick a vault, a scrypt-storm freeze, an unvalidated server-advertised OAuth scope, a cross-window key-purge race, a one-shot broker-start failure, a MAC-tamper that healed itself (now fail-closed), an unbounded grant map, and a blocking audit write. Nothing deferred — finding 4’s deeper fix (retire the v1 vault envelope) also shipped |
| [SECURITY_REVIEW_2026-08-24.md](SECURITY_REVIEW_2026-08-24.md) | 2026-08-24 — the pre-launch review of the whole product, public server included. Three CRITICAL findings, each reproduced before it was fixed: two shell/argv injections through an entity’s `host` field, and a share size cap that could be walked around into a repeatable server OOM. Git history scanned for secrets across every commit: clean |
| [SECURITY_REVIEW_2026-08-23.md](SECURITY_REVIEW_2026-08-23.md) | 2026-08-23 — security, reliability and architecture. Five high findings fixed with a red-first test each; the rest are plans |

## Implemented plans

Newest first.

| Plan | Landed | What it delivered |
|---|---|---|
| [PLAN_cross_window_write_coordination.md](PLAN_cross_window_write_coordination.md) | 2026-09-03 | Two windows of one profile no longer overwrite each other. Contains its own refuted design in full, which is the more useful half: a lease key in `globalState` with a write-then-read-back, killed by three Blocking findings from three vendors before a line existed, because that store has no compare-and-swap. What shipped rests on `fs.mkdir` without `recursive` — the one call that cannot half-happen — plus a heartbeat (a killed window frees the lock; a slow one keeps it), a fenced release (a broken holder cannot delete its successor's lock) and a sweep that skips while a peer works and comes back in case that peer dies. The residual race is named in the primitive's own header. |
| [RETRO_payment_session_2026-09-03.md](RETRO_payment_session_2026-09-03.md) | 2026-09-03 | Seven mistakes from one long session, and the finding that matters more than the list: **three of them were already forbidden by a rule that exists**. The one that shipped was a review finding fixed at the single site the reviewer quoted while the same shape sat on four more — which `security.md` has covered since before the session, under a heading whose four examples are all escapers, so the reader with a concurrency finding does not see themselves in it. Two were real gaps (a merge that produced a valid-and-wrong CHANGELOG the release workflow would have mis-sliced; a deviation recording something as impossible without naming what it had checked, which deferred the Monero wordlist for a month on a conclusion that did not follow). Ends with three proposed changes to the shared conventions, each a widening rather than a new file. |
| [RESULTS_review_gate_payment_ui_tail.md](RESULTS_review_gate_payment_ui_tail.md) | 2026-09-02 | What the multi-model review gate actually caught on one feature, counted rather than felt: **30 findings, 16 acted on, 8 defects prevented**. The number that changes how the gate is read is the per-vendor split — `codex` 13 findings / 12 changes, `gemini` 5 / 4, and the local engine **12 findings / 0 changes**, five of them pure commentary. Two rejections there asked for the opposite of an accepted finding and for the enumeration hint the design exists to withhold. And the two defects that would actually have DESTROYED data came from neither round — the phrase form's own round-trip tests found them, which is the argument for the gate being additional rather than a substitute. Test evidence for the release is recorded alongside. |
| [PLAN_payment_ui_tail.md](PLAN_payment_ui_tail.md) | 2026-09-02 | The read side the two plans above shipped without, found by an audit that read the IMPORTS rather than their story table: six modules with no production caller, a viewer that did not contain the string `payment` at all, and a *Phrase* option in the form selector with no fields behind it. It adds the read-only card (drawn from metadata, so no stored value is ever built into the page's HTML string), the method picker that rebuilds a woven field into two rows and hints at nothing, the reveal rung on CVV/PIN/an assembled phrase — covering the METHOD PICKER and not only the Show button, which is what the plan review caught — and the phrase form. Two silent-destruction defects in already-green code surfaced on the way: a woven phrase's mark was pruned by the rule that keeps marks honest, and `hasMixedField` did not recognise a phrase at all, so a saved one would have been editable and re-woven on the next save. |
| [PLAN_payment_instruments.md](PLAN_payment_instruments.md) | 2026-09-02 | The ninth entry kind: a card, bank details, or a phrase — one encrypted record under one key, and any field storable WOVEN with a decoy under a method kept nowhere. Read the status line for the six deviations; the two worth knowing are that its §3d turned out to be a change to every kind (it became S1.4 and ten review rounds), and that the decoy rule was corrected before shipping — the decoy matches the entered phrase's checksum STATE, because under the old "always converge" rule a phrase whose words the person had moved themselves would have had exactly one validating half. |
| [PLAN_payment_instruments_epics.md](PLAN_payment_instruments_epics.md) | 2026-09-02 | The build order for the above: 5 epics, 21 stories, one commit each, with the per-story record of what each review round found. The recurring finding is worth more than any single story — FOUR safety nets shipped with no caller (`clearForForm`, `withheldFromShare`, `validatePayment`, `initialPayment`), the last of which deleted a saved card on every edit. |
| [PLAN_agent_folder_ops_itest.md](PLAN_agent_folder_ops_itest.md) | 2026-08-31 | The folder verbs driven end to end, on real storage rather than a stub — and the test found that the whole surface had been answering `404` since it shipped, because its listing was wired behind POST while the contract files it under `reads`. Deviation worth reading: the plan named one seam for the no-escalation check and sabotage found three in series, the strongest being the one the plan did not mention. |
| [PLAN_server_share_format.md](PLAN_server_share_format.md) | 2026-08-31 | Sharing through a vault server, which had been impossible since 0.82.1 and said so in the sender's name. A share's binding form now comes from its transport, and the server carries the `format` that names it (contract 2). The deviation is the part to read: the fix's own nullable field would have emptied every un-updated client's inbox, and the hand-run integration test nobody had run in six days is what caught it. |
| [PLAN_audit_roadmap_2026_08_25.md](PLAN_audit_roadmap_2026_08_25.md) | 2026-08-28 | The 2026-08-25 audit's roadmap — every block closed or decided; the last two, A1 (`extension.ts` 5689 → 1105) and A4 (the shapes union), on the day it was promoted. |
| [PLAN_server_ops.md](PLAN_server_ops.md) | 2026-08-28 | The server's operational hardening, all nine items — the last three (network-FS refusal, officers' metrics + runtime line, cached health) and the byte budget shipped as server 0.4.0. |
| [PLAN_share_metadata_aad.md](PLAN_share_metadata_aad.md) | 2026-08-28 | A share's label bound to its ciphertext as GCM AAD; legacy shares open, marked unbound, until 0.85.0. |
| [PLAN_ephemeral_secrets_tail.md](PLAN_ephemeral_secrets_tail.md) | 2026-08-28 | The ephemeral-secrets tail: *Burn Now…*, the lifetime on the card, and the cross-machine burn proven by test. |
| [PLAN_tails.md](PLAN_tails.md) | 2026-08-28 | The 2026-08-27 audit of every open plan plus the owner's asks — 34 tails shipped as 0.80.0 → 0.81.0; each item records what shipped and how it deviated. |
| [PLAN_extension_security_tail.md](PLAN_extension_security_tail.md) | 2026-08-28 | The 2026-08-23 review's extension findings: PIN policy, idle auto-lock, owner-only ACL, and the WebAuthn RP ID moved to `creds-for-devs.localhost` with legacy keys re-registered on first use. Finding 7 extracted to `todo/PLAN_share_metadata_aad.md`. |
| [PLAN_marketplace_listing.md](PLAN_marketplace_listing.md) | 2026-08-24 | The Marketplace listing text and metadata; the screenshots remain a human task. |
| [PLAN_logging_convention.md](PLAN_logging_convention.md) | 2026-08-27 | The server's logging brought level with the family rule: the hand-written `AnsiConsoleSink` ported (Serilog's console themes measurably write zero escape bytes once stdout is redirected — and a container's captured stdout always is), `DailyRunFileSink` segmenting a never-restarting run at UTC midnight with the same pid, and `LogRetention` as the named owner of `logs/` — 14 days, adopted from the extension's `diagnosticLog.ts` because one product gives one answer. The record worth reading: its item 2 was **void, not done** — it asked for a row in a mirror list the shared rule no longer has, the rule having moved into the submodule this repository already mounts |
| [PLAN_config_sharing.md](PLAN_config_sharing.md) | 2026-08-27 | Sharing a config carries the document — extracted from the plan above and shipped the same day, because the owner tried it and the JSON did not survive. The body travels sealed; `shareableDetails` strips `configKeyHash` as its sixth field, since a key minted for one vault is one the recipient can neither use nor revoke. **The record worth reading is the hole found on the way**: `setPassword(undefined)` means "keep what is stored", so an entity converted from a credential into a config kept an invisible password — and `isShareable` returned true for anything with one, which made that config shareable ALREADY and would have delivered the password with the document left behind. `keepsPassword(kind)` scrubs it on write, the rule TOTP had and configs did not. Also found and deliberately not fixed here: `buildSharePayload` never reads the TOTP seed |
| [PLAN_config_entities.md](PLAN_config_entities.md) | 2026-08-27 | The `config` entity kind: `appsettings.Development.json` lives in the vault instead of being passed between developers by hand and lost. Its BODY is a secret in SecretStorage — a script body is what a person typed at a shell, a config body holds connection strings with passwords in them — so there is no field on `EntityMetadata` it could sit in, and a test asserts there is none. Validation saves anyway and marks the row `!!!` until it parses; the Fields tab is a VIEW over the raw text, splicing one value's span rather than reassembling the document, so it structurally cannot lose your indentation or your comments; materialising to disk refuses a path git TRACKS and asks about one it does not IGNORE. Reachable from code through a long-lived per-entity key of which only a SHA-256 is kept — not a grant, because grants die with the window and carry its port — served by `POST /v1/config/read` and `creds config <key>`, with no consent modal and every attempt in the audit log as a fourth door. **The record worth reading is deviation 1**: the .NET provider package this plan called for was never built. The owner asked whether an instruction would do, and checking rather than answering showed .NET already ships `AddJsonStream` — the package would have wrapped ten lines and bought a fifth release line, a public API and a version story. A twenty-language snippet panel in the viewer replaced it and covers every language instead of one. Tail: [PLAN_config_sharing.md](PLAN_config_sharing.md), shipped the same day |
| [PLAN_cli_bridge_tail.md](PLAN_cli_bridge_tail.md) | 2026-08-27 | The tail the CLI and bridge plans left, closed by the one thing no test could reach: a human clicking *Open Remote Bridge*. It was written as a formality — the transport had already been measured against a real sshd, and what remained was "confirm the command calls it". The click found the bridge had never worked at all, for four independent reasons, each sufficient alone: the credential resolver knew one kind of four, so a password entry — the case the bridge exists for — got an `ssh` with nothing to authenticate with; no `BatchMode`, so the resulting refusal became a permanent wait at a password prompt on a pipe, with a live process and an ESTABLISHED connection; a socket check that saw the absence and filed it at info level, having collapsed "no socket" into "this host has no `stat`"; and a notification announcing success before any of it was checked. A fifth was found by the owner in the same click — the menu item still read *Open* while a bridge ran. The generalisable part is in the plan: §1 was marked "not verified" honestly and still read as a small remainder, and four defects in the single unverified path is the price of the line "covered by tests individually" |
| [PLAN_agent_folder_ops.md](PLAN_agent_folder_ops.md) | 2026-08-28 | Folders become the second object on the agent surface: four more switches (create / rename and move / delete what it made / delete any), a second ladder meeting the first only at *visible*, and four tools — `creds_folders`, `creds_create_folder`, `creds_edit_folder`, `creds_delete_folder`. Two things are worth reading it for. **An agent can never change a permission, and that is a property of the SHAPE**: no request it can compose has a field the switches could arrive in, on either side of the wire. And a move needs the grant at BOTH ends — a folder passes its answers to everything inside it, so moving one is a permission change for its contents. It also unified inheritance (creation used to resolve against a folder's own switch while everything else climbed, which is why `can.create` could say yes and the create call refuse) and restored `mcpConfigSnippet`, a contract route the generator had quietly stopped emitting. Its tail shipped 2026-08-31: [PLAN_agent_folder_ops_itest.md](PLAN_agent_folder_ops_itest.md) |
| [ЗАДАЧА_проверка_mcp_папки_и_генерация.md](ЗАДАЧА_проверка_mcp_папки_и_генерация.md) | 2026-08-29 | The tail of the pass above, covering what came after 0.78.1: folders (0.85.0) and the generation options (0.86.0). Two steps carried the weight and both passed — an agent putting `mcp` in the body of `creds_edit_folder` left the switches untouched, which is what verifies the protection is STRUCTURAL rather than merely remembered; and a password asked for with no character sets was refused rather than stored empty. It also found the thing only a reader of the instructions would notice: the server `Instructions` — the first text a model sees — never mentioned folders at all, so a model following them would never reach the second object. Fixed the same day; it needs an `mcp-v*` tag to reach anyone |
| [ЗАДАЧА_проверка_mcp_вручную.md](ЗАДАЧА_проверка_mcp_вручную.md) | 2026-08-29 | The manual pass over the MCP surface as it stood at 0.78.1, confirmed by the owner. Kept because of what it FOUND rather than for its ticks, and all three findings were the kind no unit test reaches — each lives at a seam between two halves. `can.create` said yes while the create route said no, because one end climbed the tree for inheritance and the other did not. The broker's listener opened only on the first *Share with Claude Code*, a door MCP never uses, so every reload left an agent told "no window answered" while the window was open. And from WSL the agent could not reach the window at all. All three are fixed and released. What it does NOT cover is written into it: folders (0.85.0) and the generation options (0.86.0) came after, and are [ЗАДАЧА_проверка_mcp_папки_и_генерация.md](ЗАДАЧА_проверка_mcp_папки_и_генерация.md) |
| [PLAN_mcp_server.md](PLAN_mcp_server.md) | 2026-08-27 | Roadmap **D8**: the broker shows itself to agents over MCP — all five levels in the extension, the `creds-mcp` binary, eight tools, an MCP journal and both install buttons, across 0.65.0–0.75.0. Its own tail, the WSL bridge, shipped 2026-08-28 ([PLAN_mcp_wsl_bridge.md](PLAN_mcp_wsl_bridge.md)): the Linux binary hands the whole session to `creds-mcp.exe` through interop and carries its stdio, so an MCP client inside a distribution reaches the window on Windows with no configuration |
| [PLAN_org_recovery.md](PLAN_org_recovery.md) | 2026-08-27 | Corporate break-glass recovery: an operator names recovery officers, every vault on that server also seals its master key to an X25519 escrow wrap, and any quorum of officers can open a departed colleague's vault — the organisation's key split with a hand-written GF(2^8) Shamir and destroyed at the end of setup. Three records worth reading, each a thing the design got wrong on paper: a throwaway spike refuted the JWK route for raw private keys before a line was written (importing one is refused without its public half; DER has no such asymmetry), the recovery must try SUBSETS of the contributions because interpolation over a wrong one returns a well-formed WRONG key rather than failing, and the server's threshold gate is a courtesy it structurally cannot enforce — it cannot tell a genuine contribution from a blob, so the client's integrity tag is the real gate. The tail it did not build is [../todo/PLAN_org_recovery_tail.md](../todo/PLAN_org_recovery_tail.md) |
| [PLAN_qr_seed_paste.md](PLAN_qr_seed_paste.md) | 2026-08-27 | The one-time-code seed arrives as a **pasted picture**, because that is the only form Google Authenticator will ever let one out in (Microsoft Authenticator lets one out in no form at all). A QR reader written from ISO/IEC 18004 in five modules — thresholding and the finder search, then the format code, the mask, the block layout and Reed-Solomon — so the extension keeps its zero runtime dependencies, which the owner chose over `jsqr` explicitly. What makes that safe is the fixture: forty-one real payloads (app stores, a device sign-in, a cafe's wi-fi and menu and card, SEPA, UPI, a poster campaign, Ukrainian and Japanese text, and Google exports) encoded by a **third-party** encoder, so the standard's tables are checked against somebody else's reading of the standard. Four bugs the fixtures found are recorded in it, each invisible to the case above it; the one worth reading is that Berlekamp-Massey written with an `else` passes every clean symbol and repairs nothing |
| [PLAN_recovery_code_wrap.md](PLAN_recovery_code_wrap.md) | 2026-08-27 | The printed recovery code — 150 bits of Crockford Base32 with a checksum that names a mistyped character locally, wrapped by HKDF as a third `KeyWrap` kind, shown once on a page with Print and deliberately no Copy. Two defects found while building it, both red-first: `backupWriteMode` routed by "has a webauthn wrap" and would have silently stripped the new slot on a NAS backup, and removing your last security key retired a registered code **in silence** — the discard is unavoidable arithmetic (re-wrapping needs the code, which is on paper), so the silence was the defect. Master-key rotation stopped being inlined twice and became `vaultRekey.rekeyUnderPin` |
| [PLAN_headless_cli.md](PLAN_headless_cli.md) | 2026-08-26 | `creds`: the product's terminal half, outside VS Code entirely — .NET 10 Native AOT, all eight verbs, named grants (`Enable CLI Access…` plus a `POST /v1/alias/<action>` route), and window discovery through `endpoints/window-<pid>.json`. The binary holds no credential and can obtain none: there is no field in the protocol a secret could travel in, which is a property of the response types rather than a promise. `creds ls` was deliberately absent at first — an enumeration route lets a caller compute which names exist — and was added afterwards under the owner's recorded decision, because the CLI that most needs it runs on a Remote-SSH host where the registry lives on the machine at the other end and cannot be read from disk. Four RID builds ship on a `cli-v*` tag; the first release is 2.3–3.1 MB compressed, 6.8 MB on disk, and needs no runtime on the host |
| [PLAN_remote_broker_bridge.md](PLAN_remote_broker_bridge.md) | 2026-08-26 | The broker reached from somewhere that is not this machine: a second listener beside the loopback TCP one (a 0600 unix socket on POSIX, a named pipe on Windows), WSL served by interop-exec — the Linux `creds` detects WSL by two independent signs and hands the call to the Windows binary, relaying streams and exit code — and Remote-SSH served by an `ssh -R` forward. The socket's permissions on the remote are the boundary and we cannot set them: for a `-R` forward **sshd** creates the socket, so the server's `StreamLocalBindMask` governs and the client flags that appear to ask for it were measured doing nothing. So the bridge looks instead of assuming. Phase 4 waits on D1 |
| [PLAN_wsl_agent_relay.md](PLAN_wsl_agent_relay.md) | 2026-08-26 | An `ssh-agent` usable from inside WSL while the key stays on the Windows side and never crosses. Two new verbs in `src_cli/` and an address published by the extension; the agent answers signature requests, so what reaches the VM is a signature and never key material — the distinction the whole product rests on, made observable in the one place a person can watch it work |
| [PLAN_depends_on.md](PLAN_depends_on.md) | 2026-08-26 | An SSH host is unreachable without the VPN that opens its network, and the vault held both entries and nothing about the sentence joining them. An entity now says what it `dependsOn`: both ends wear one colour in the tree, and the entity depended ON grows a *Depended on by* sub-tree **beside** its version history — the coexistence is a test, not a hope. The colour is stamped on the TARGET, never on the edge, which is why "change it once and every dependent follows" needs no propagation code and cannot drift. Two things worth reading: colour borrowing is **one hop, never transitive** — pinned after a test of mine failed asserting the opposite, because in a vault where everything reaches one VPN the chain would paint everything alike — and a dangling reference is **kept, not swept**, since a deleted target may be one sync away from returning. Colouring a row's LABEL needed `FileDecorationProvider`, an API this extension had never used; it was built as a spike and looked at before anything was designed on top of it |
| [PLAN_ephemeral_secrets.md](PLAN_ephemeral_secrets.md) | 2026-08-26 | Entries that delete themselves — after an hour, a day, when VS Code closes, or once an agent has used them — through the single deletion path, so the secret, its revision history and a causal tombstone all go together. The deviation is the interesting part: *"until the window closes"* became a **lease the open window renews**, because a close handler never runs for a window that crashed, and the entry it promised to destroy would then outlive its own promise while still holding a working secret. `oneUse` is refused for an SSH key pair, which the broker never serves and so could never burn. Its tail is [PLAN_ephemeral_secrets_tail.md](PLAN_ephemeral_secrets_tail.md) |
| [PLAN_ai_context_masking.md](PLAN_ai_context_masking.md) | 2026-08-26 | Vault secrets redacted out of Exec Broker output before it can reach an AI's context, at one interception point that covers every action present and future. The clipboard half is recorded as **refuted rather than deferred** — VS Code exposes no clipboard-change event and Windows Clipboard History captures at copy time, so a watcher would be a promise the platform cannot keep — and is replaced by two on-demand scans. Two later defects are recorded in it: MSSQL passwords never entered the mask table (an existing test had pinned that as intended behaviour), and an entry name could smuggle instructions into the agent snippet |
| [PLAN_git_sync_transport.md](PLAN_git_sync_transport.md) | 2026-08-25 | A third transport beside the shared folder and the server: the encrypted vault in a private git repository, reusing the causal merge untouched — the clone is a cache, and a rejected push is the server transport's `412`. Two things it does not hide: a repository's log reveals *when* a vault changed however opaque the files are, and deleting a vault removes the file, not the history. History retention and the PAT path were deliberately left out. Its integration test, against a real bare repo, caught the one defect no unit test could — git rewrites line endings on checkout, which would have corrupted every vault stored on Windows |
| [PLAN_broker_itest_defects.md](PLAN_broker_itest_defects.md) | 2026-08-25 | Two defects that reached released builds and stayed there: a refused agent token answered *unknown* instead of *denied* once any later grant was minted (the registry swept every refusal), and three assertions about decrypted key material tested nothing because the test helper never looked inside `keys/<pid>/` — one could not pass, two could not fail. The plan's own diagnosis of the second was wrong and the correction is recorded in it. The finding worth more than either: the only test that drives the real broker ran in no CI, and now does |
| [PLAN_import.md](PLAN_import.md) | 2026-08-25 | Import from `~/.ssh/config` and from Bitwarden / 1Password / KeePass / LastPass / Termius exports — the migration cost the audit named as the main obstacle to adoption. Format detected by content, skipped rows always reported, fresh ids for everything. KDBX is deliberately absent, and the module says why: Argon2 is not in Node, and this extension has no runtime dependencies |
| [PLAN_generator.md](PLAN_generator.md) | 2026-08-25 | Passwords, passphrases and Ed25519 keys generated in the form. The passphrase list is exactly 256 words so "eight bits per word" is true rather than approximately true, and decoration that satisfies a composition rule is not counted as strength. A generated key never touches disk — which `ssh-keygen` cannot do by construction |
| [PLAN_hygiene_reports.md](PLAN_hygiene_reports.md) | 2026-08-25 | The health report: reused passwords, weak ones, unencrypted keys in `~/.ssh`, plaintext credentials in a workspace `.env`, and an opt-in breach check that sends five hex characters of a SHA-1 and nothing else. No finding ever contains the value that caused it — the first test in the file |
| [PLAN_polish_layer.md](PLAN_polish_layer.md) | 2026-08-25 | The convenience layer the UX audit found entirely missing: five keybindings, a go-to-credential picker that matches exactly what the tree filter matches, `viewsWelcome`, a walkthrough, and a status-bar item for the lock — the state that decides whether sync runs at all |
| [PLAN_connection_manager.md](PLAN_connection_manager.md) | 2026-08-25 | The connection-manager fields an SSH entity was missing — a jump host as a typed reference, -L/-R forwarding rules, agent forwarding, tags — and audit B10 closed: a first connection shows the host fingerprint before it is trusted, and a pinned key is enforced rather than warned about. The first attempt got the host-key rule backwards (accept-new on the human path too, weaker than ssh own default); its test caught it |
| [PLAN_ssh_agent.md](PLAN_ssh_agent.md) | 2026-08-25 | An SSH agent of our own: keys served from memory over a per-window socket (a named pipe on Windows), a confirmation in front of **every** signature that names what is being signed, `SSH_AUTH_SOCK` injected into new terminals, and Git commit signing with a key that exists in no file. Two findings came from verifying rather than reasoning — Git-for-Windows' MSYS `ssh-keygen` cannot reach a named pipe (so the config sets `gpg.ssh.program`), and the `-Y sign`/`-Y verify` round trip is what proves the signature encoding. "Load into a running agent" is deliberately not built |
| [PLAN_secret_references.md](PLAN_secret_references.md) | 2026-08-25 | `creds://` references and a masked run wrapper: a stored command or script names a credential instead of holding one, the value reaches only the child's environment, and everything the child prints is scanned for it. Deviated from the plan by keeping the broker's `env` verb as the documented weaker option rather than retiring it |
| [PLAN_totp.md](PLAN_totp.md) | 2026-08-25 | TOTP as a stored secret field with a live code in the viewer. The second deliberate exception to "the viewer never receives a secret" — the derived code travels, the seed never does — for the same reason the image preview was the first |
| [PLAN_v1_vault_migration.md](PLAN_v1_vault_migration.md) | 2026-08-25 | Retired the v1 vault envelope: every vault is written v3 (wrapped/HKDF, scrypt once at unlock) — a legacy PIN-only vault migrates on its next sync with the same PIN and no data loss, a new PIN-only vault is v3 from the first write, and backups convert on their next run keeping their own standalone PIN. Closes the deferred tail of the 2026-08-25 review's finding 4 |
| [PLAN_nas_sender_pki.md](PLAN_nas_sender_pki.md) | 2026-08-25 | Sender authenticity for the folder transport: Ed25519 signatures over a transcript that binds recipient, payload and the sender’s own key, pinned on first contact. Shipped with the ceiling stated rather than hidden — trust-on-first-use is weak against an attacker already in place, and only the out-of-band fingerprint closes that |
| [PLAN_agent_ssh_broker.md](PLAN_agent_ssh_broker.md) | 2026-08-24 | *Share with Claude Code…* — an AI coding agent can run commands on an SSH host and open its terminal without ever receiving the password or key: it holds a capability token, and the window that holds the credential runs `ssh` on its behalf, gated by a first-use consent modal and recorded in an audit channel. Two claims did not survive contact: `BatchMode=yes` under forced askpass (unproven, so the password branch avoids depending on it) and per-call key cleanup (a finished exec deleted the file a concurrent one was still using) |
| [PLAN_monorepo_consolidation.md](PLAN_monorepo_consolidation.md) | 2026-08-23 | Both products into one conventions-compliant repository: the submodule mount and .NET baseline, the move (134 MB → 614 KB), the server's test suite converted to in-process xUnit v3, CI for both halves separately, MIT licensing and Marketplace preparation, the one-command Docker stack with four TLS modes, and the review above. Three defects surfaced only by verifying rather than reasoning — a Linux-only first-boot crash from a root-owned bind mount, `nginx:alpine` having no `openssl`, and nginx's stock `default.conf` shadowing the API |
| [PLAN_sharing.md](PLAN_sharing.md) | 2026-08-21 (v0.13.0) | Sharing one sealed entity with a colleague. Deviated from the plan: shares live as a plaintext array inside the recipient's envelope rather than one file per share. Its open residual — an unauthenticated sender label on the folder transport — is finding 7 of the security review |
| [PLAN_audit_followups.md](PLAN_audit_followups.md) | 2026-08-21 (v0.19.0–v0.22.0) | The six follow-ups from the 2026-08-21 audit that each needed a decision or a data migration: KDF parameter versioning, causal version-vector merge, the envelope MAC, PIN re-key, remote vault deletion on account removal, and notes moved into SecretStorage. Handed the server-side operational items to `../todo/PLAN_server_ops.md` |

## Conventions

The family-wide rules are a submodule at `.claude/rules/shared`. Repository-specific rules are in
[../CLAUDE.md](../CLAUDE.md). The `research/` + `todo/` split, and the promotion procedure that moves
a plan between them, are defined in `.claude/rules/shared/common/planning-docs.md` and enforced in CI
by `plan-lifecycle.mjs`.
- [PLAN_payment_polish_and_entity_pin.md](PLAN_payment_polish_and_entity_pin.md) — the owner's batch of 2026-09-03: the payment surfaces, the weaving controls, the billing address as cells, generation with parameters, and the secret envelope §6 rests on.
